#!/usr/bin/perl

package json2yaml; # make PodWeaver happy

use 5.010;
use strict;
use warnings;

use YAML::Syck; $YAML::Syck::ImplicitTyping = 1;
use JSON;

# VERSION

undef $/;
print Dump(from_json(scalar <>));
