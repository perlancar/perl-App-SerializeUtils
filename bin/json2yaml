#!/usr/bin/perl

package json2yaml; # make PodWeaver happy

use 5.010;
use strict;
use warnings;

use JSON;
use YAML::Syck; $YAML::Syck::ImplicitTyping = 1;

# VERSION

undef $/;
print Dump(JSON->new->allow_nonref->decode(~~<>));

1;
#ABSTRACT: Convert JSON to YAML serialization
