#!/usr/bin/perl

use 5.010;
use strict;
use warnings;

use JSON;
use PHP::Serialization qw(unserialize);

# VERSION

undef $/;
print to_json(unserialize(scalar <>));

1;
#ABSTRACT: Convert PHP to JSON serialization
#PODNAME: phpser2json
