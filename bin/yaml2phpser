#!/usr/bin/perl

use 5.010;
use strict;
use warnings;

use YAML::Syck; $YAML::Syck::ImplicitTyping = 1;
use PHP::Serialization qw(serialize);

# VERSION

undef $/;
print serialize(Load(scalar <>));

1;
#ABSTRACT: Convert YAML to PHP serialization
#PODNAME: yaml2phpser
