#!/usr/bin/perl

package json2dd; # make PodWeaver happy

use 5.010;
use strict;
use warnings;

use Data::Dump;
use JSON;

# VERSION

undef $/;
dd(from_json(scalar <>));

1;
#ABSTRACT: Convert JSON to Data::Dump serialization
