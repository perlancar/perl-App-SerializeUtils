#!/usr/bin/perl

package json2php; # make PodWeaver happy

use 5.010;
use strict;
use warnings;

use JSON;
use PHP::Serialization qw(serialize);

# VERSION

undef $/;
print serialize(from_json(scalar <>));

1;
#ABSTRACT: Convert JSON to PHP serialization
